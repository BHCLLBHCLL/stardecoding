# S 波规划：官方桥实战 × 求解精度攻坚 × 语义深化

> 日期：2026-09-14。基准：Simcenter STAR-CCM+ 2502（20.02.007，本机**带 license 实测可用**）+ 官方 Javadoc HTML + 用户指南。
> 依据：本次代码/测试/语料复核（840 项测试 / 68 文件全绿）、R 波 R1–R6 交付物、以及本轮新增实测证据（官方批量宏跑通、圆柱涡脱未复现）。
> 口径沿用 parity_100pct_plan.md §0：L0 外壳 / L1 会话 / L2 落盘 / L3 正确。

## 0. 本轮复核的新事实（决定优先级）

| # | 事实 | 证据 | 影响 |
| --- | --- | --- | --- |
| F1 | **官方 STAR-CCM+ 2502 带 license 完全可用**：批量宏在工作副本上跑通并回传结果（`returncode=0`） | `starccmw.exe -version` → 20.02.007；`star_macro.run_star_macro_on_copy` 探针日志：`License build date: 28 October 2024` + `STARDECODING_PROBE_OK name=work` + `STARDECODING_PROBE_REGIONS 1` | **B 路线从"就绪未测"变为"实测可用"**：官方差分/官方参考数据/宏桥均可执行 |
| F2 | 语料比文档所述更富：openfoam/benchmark 下约 25 个含真实解场的 .sim（20245 单元 2D 圆柱 + 升力监视器 + 残差） | G5/G6 抽取；R4 解出官方 St=0.1752 / Re=200 / 残差 3.2e-10 | 求解侧"官方对标"不缺数据，缺的是**我们的复现能力** |
| F3 | mesh_tet 分辨率由输入表面离散主导（spacing 只控内部填充点） | 管道域 h=0.04/0.08/0.12 实测均 8118 单元 | R5 的 tet 对标提速必须先粗化表面 |
| F4 | 结构化网格（O 型/笛卡尔阶梯）秒级生成、体积守恒 0.03–0.11% | R4 channel_tet_mesh*，24432 单元 | 自研算例的网格瓶颈已解除 |
| F5 | **圆柱涡脱未复现**：24432 单元 × 120 步 × dt=0.2s（约 5.2 官方周期）→ Cl 振幅 0.0043、后半段衰减 7.3e-5（官方 0.2805） | R4 实跑 592 s | 求解深度是**最大短板**：需二阶对流 + 近壁加密 + 更长积分 |
| F6 | T 载荷结构层 99.86% 分解，语义仅 18.3% | R3 t_stream_report：归属 18.33% + 双精度串 39.49%（命中对象值 25.7%）+ u16 串 42.04%（命中 id 32.2% vs 基线 15.9%） | 解析深化的下一刀是**字段级归属** |
| F7 | 解析侧新缺口：vortexShed_tutor_v3_0.025.sim 体网格抽取失败（17465/20245 单元无面） | 同族 v3_0.05_2502 正常；线索 FvRegion 1 vs 2、DUP 组 7 vs 14 | 影响多算例官方对标 |

## 1. 与 STAR-CCM+ 20.02 的能力对照（12 维，本次复核更新）

| # | 维度 | 完整度 | 深度 | 本轮变化 | 关键证据 | 主要缺口 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 文件格式解析（逆向） | 90% | 84%→**85%** | ↑ | 21/21 解析；T 载荷结构分解 99.86%、语义 18.3%；数组标注 91.3% | 字段级语义、尾部校验和、NameManager、v3_0.025 抽取 |
| 2 | 文件写入 / 落盘往返 | 96% | 88% | — | W1–W6；本仓库重开往返全绿 | **官方差分从未真跑**（license 现已可用） |
| 3 | CAD 几何内核 | 85% | 78% | — | C1–C6 OCC 构造/编辑/修复/包裹 + STEP/IGES/BREP 双向 | T 块读侧 B-Rep；Parasolid 写仅 B 路（可实测）；缺官方 B-Rep 对标 |
| 4 | 网格内核 | 90%→**91%** | 85% | ↑ | N1–N6 全谱；**R5 教程尺度对标框架**（域表面/补丁四项 + 退化保护） | tet 路径慢（需表面粗化）；interface 语料缺；prism/AMR 未入求解环 |
| 5 | 区域/边界/界面与对象图 | 92% | 82% | — | G4 22 边界/11642 面精确闭合 | 面网格 patch 仍启发式；跨 part 聚合待细化 |
| 6 | 物理/材料/模型谱系 | 88% | 80% | — | G7+P1 语义读写闭环；R4 抽 U/ρ/A/D/Re | 22+ 模型族未逐一解码；材料库/EOS 仅常数 |
| 7 | 场函数/初始化/数据映射 | 80%→**82%** | 74%→**75%** | ↑ | P2 求值器、P3 初始化器、R2 跨网格映射 | 映射未接入求解环；DerivedDataSet 仅标注 |
| 8 | 求解器内核 | 93% | 82%→**81%** | **↓ 诚实重估 → S2 后维持 81%** | P4–P12 全谱 + R1 回归 + R4 官方参考与实跑 + **S2 对流格式/滑移壁（零回归，15 项测试）** | **涡脱仍未复现**：三路手段（二阶格式/滑移壁/8 单元每直径）均衰减 → 瓶颈=时间推进隐式平滑 + 算力（24k 单元 4.3 s/步、45k 单元 14 s/步） |
| 9 | 求解运行控制与监视 | 95% | 85% | — | P10 Run/Step/Stop + 监视/残差曲线 | B 路宏未实测（现可）；无求解断点/续算 |
| 10 | 后处理与场景可视化 | 95% | 88% | — | V1–V7 + Post 20 动作；CSV/EnSight/CGNS；离屏动画 | 场景内嵌绘图面板未解码；mp4 需 ffmpeg |
| 11 | 自动化生态 | 60%→**62%** | 52% | ↑ | A1 宏录播 / A2 star API / A3 DOE；**R6 协同配置前段 + Javadoc 核对宏** | A4 伴随 / A5 协同协议 / A6 HPC 挂起；语料 0 个 cosim 对象 |
| 12 | 客户端体验 | 95% | 86% | — | X1–X4 + F0–F8；66 项动作/18 菜单 | 打包无 PyInstaller；3D-CAD 界内建模、Mesh 清除/转 2D 缺 |

**汇总：完整度 ≈ 89% · 深度 ≈ 80%（净持平：解析/网格/映射小幅上调，求解器内核按实测下调 1 点）。**
本轮 R 波的净贡献是**验证基础设施 + 诚实的缺口量化**，不是能力面扩张 —— 避免"文档涨分、能力不涨"。

## 2. 三个深度空档（新一轮靶心）

- **空档 A（解析语义）**：结构已通（99.86%），语义停在 18.3%；数组字段级 91.3%（余 121 块）；尾部校验和、NameManager、v3_0.025 抽取失败。
- **空档 B（求解精度）**：涡脱未复现（F5）；性能不足以支撑 10 万+ 单元；无并行/GPU。
- **空档 C（自动化/协同）**：A4–A6 挂起；协同仿真语料为零。
- **新机会（F1）**：官方 license 可用 → 官方差分可闭合、官方参考数据可自产、字段语义可用官方导出对照。

## 3. S 波关键提升点

### S1 官方桥实战化与官方差分（P0，依赖 F1）—— ✅ 2026-09-14 达成

**实测证据（本轮，全链跑通）**：

| 步骤 | 结果 |
| --- | --- |
| ① 本仓库 Save As（把 Fluid Domain 改名为 S1 Patched Name） | 写出 561,413 字节；本仓库重读命中改名 |
| ② 官方打开我们的产物并输出视图统计 | Region 1=1、Scene 1=1、Continuum 1=1、Report 0=0、Plot 1=1、Monitor 8=8（**6/7 类 Δ=0**；Part 类口径不同，标注近似） |
| ③ 官方对我们产物 Save As（重存） | 成功（official_resaved.sim 生成，无报错） |
| ④ 名字键差分（官方重存件 ↔ 我们的产物） | 配对 **550/664 = 82.8%**；**值型差异 0**；引用型 478（官方重编号 id，预期）；归一化 7（官方补默认值/预定义查找表按类表达）→ **结构与值型字段全稳定** |
| ⑤ 我们的编辑是否穿过官方重存 | **命中 1 处 S1 Patched Name** —— 编辑在官方客户端往返后仍在 |

**关键结论（差分口径，已实现进模块）**：官方重存会重编号对象 id（Parent/Keys 必变）并物化额外对象（+509）/替换已弃用类（-114），因此官方差分必须**按「类名+名字」配对**，差异分三类：**值型**（判定依据）、**引用型**（id 重编号，不计失败）、**归一化**（官方补默认值/预定义表）。

**落地**：`star_bridge.py`（探测 / 宏模板 / 工作副本运行 / 视图对照 / 重存 / 名字键差分）；`sim_writer.try_official_resave` 接入安装探测（W6 的 auto-skip 在本机变为真跑）；`tests/test_star_bridge.py`（9 离线 + 3 官方门控，需 STARDECODING_OFFICIAL=1）；`self_test.py` S1 离线锚点 ALL CHECKS PASSED。

**W6 官方差分转真跑（本轮附带修复）**：`tests/test_diff_regression.py::test_w6_official_resave_gated` 现在**真的调用官方 starccmw**（`returncode=0`，17 s 内跑完 4 项）——修复两处：① 子进程必须 `encoding="utf-8", errors="replace"`（官方输出含非 cp1252 字节，旧代码在读取线程抛 UnicodeDecodeError 导致 stdout=None）；② `resave_sim.java` 改为写**相对路径**（cwd 下的 `resaved_<name>.sim`），不再写死仓库绝对路径，便于在临时副本目录做差分。

**诚实边界**：① Part 类计数口径未对齐（官方 getPartManager() ≠ 对象图 *Part），不计入判定；② 官方对对象模型有归一化（物化/替换类、丢弃预定义表内联值），**不等于"文件逐字节往返"**；③ 官方集成测试默认跳过，需显式开启（每次 1–3 分钟）。
### S1 目标（存档）
- **目标**：把 B 路线从"就绪未测"变成实测闭环：① 官方批量宏冒烟（读取/统计/保存）；② 写入器输出经官方打开 + 官方重存后与本仓库重读逐项一致（W6 的 auto-skip 变真差分）；③ GUI 宏动作（生成网格/求解）在工作副本上真跑。
- **手段**：star_bridge.py（探测 exe/许可 → 宏模板 → 工作副本 → 日志回流 → 结果对比）；tests/test_diff_regression.py 的 try_official_resave 从 skip 改真跑（无 exe/许可仍 auto-skip）。
- **验收**：① 官方 -batch 冒烟 3/3 通过；② save_sim 输出被官方打开无报错，重存后对象图/数组差异仅限我们改的字段；③ 官方重存文件被本仓库重读一致。
- **诚实边界**：宏失败/许可不足一律如实报错，不 fallback 成"通过"；官方运行只在副本上，绝不改教程原件。

### S2 求解精度攻坚：复现圆柱涡脱（P0，靶心 St=0.176）—— ⚠️ 第二轮：**时间步假设被证伪**（主因定位到空间离散），仍未复现脱落

**本轮交付（零回归，可复用）**：
- **① 对流格式**：`PressureSolver(convection="upwind"|"central"|"limited")`，默认 upwind 保持不变；
  central/limited 用**延迟修正**（隐式仍上风 → 矩阵对角占优，高阶差值进 RHS），limited 走 `fvm_core.limiter`（Barth-Jespersen）。
- **② 滑移壁**：`PressureSolver(wall_slip_axes=(1,2))`（法向速度=0、切向自由；对流与扩散均无贡献、面通量恒 0）→ 准二维绕流，消除薄板侧壁摩擦。立方体上验证：均匀流 `|u|max=1.00000`、残差 1e-14；默认 `()` 时滑移面 0 → 零回归。
- 测试：`tests/test_convection.py` **15 项**（格式参数/位级零回归/三格式收敛/限制器有界/滑移壁均匀流/滑移通量零/各格式×滑移组合）；求解器回归 **109 passed**（pressure_solver / solver_run / solver_regression / turbulence / energy）。

**实跑证据（Re=200、D=0.04、U=0.05、dt=0.2、n_inner=2，Cl 振幅按前后半段对比）**：

| 配置 | 单元 | 时长 | 振幅（前 → 后） | 增长比 | 结论 |
| --- | --- | --- | --- | --- | --- |
| upwind（原配置） | 24,432 | 12 s | 0.00426 → 0.00047 | 0.11 | 衰减 |
| limited（二阶受限中心） | 24,432 | 12 s | 0.00610 → 0.00008 | 0.014 | 衰减 |
| slip + limited | 24,432 | 12 s | 0.00596 → 0.00034 | 0.057 | 衰减 |
| slip + upwind | 24,432 | 12 s | 0.00447 → 0.00082 | 0.184 | 衰减 |
| slip + limited，**h=D/8（8 单元/直径）** | 45,456 | 7.9 s | 0.00377 → 0.00190 → 0.00081 | 0.215 | 仍单调衰减 |

**对称性判别（横向速度时程，80 步 dt=0.1，PISO(2)+limited+slip）**：`max|v|` 四段峰值 0.0773 → 0.0475 → 0.0468 → **0.0461**（≈U∞，为绕柱转向流，非脱落）；`cl` 四段峰值 0.0050 → −0.0060 → −0.0020 → **−0.00053**（衰减）；`cd` 稳定 **0.047**；残差 1e-8~1e-9 → **流场收敛到稳态对称解**（不是"Cl 提取失效"，是真无脱落）。

**结论（诚实）**：本仓库压力基 SIMPLE 内核（后向欧拉瞬态 + 每步 `n_inner=2` 内迭代 + α_m/α_p 欠松弛）在 Re=200 的测试包络内**不复现涡脱不稳定性**——启动瞬态单调衰减到对称定常解。
**根因假设（可检验）**：每步 2 次内迭代 + 欠松弛使单个时间步近似"部分收敛的定常解"，瞬态项 `ρV/Δt` 相对隐式算子偏小 → **时间推进的隐式平滑压过了非定常失稳**；网格/格式只是次要因素（三路手段都改不动"衰减"趋势）。
**时间推进假设的检验（n_inner=1 vs 2，同网格/同格式/同 dt，120 步 ≈11.9 s）**：

| 配置 | 三段振幅 | 阻尼趋势 | 每步耗时（S4 提速后） |
| --- | --- | --- | --- |
| n_inner=1 | 0.00413 → 0.00265 → 0.00044 | **0.106** | 1.23 s |
| n_inner=2 | 0.00446 → 0.00061 → 0.00020 | 0.044 | 2.42 s |

→ 减小内迭代（更少隐式平滑）使阻尼**弱 2.4 倍**，**假设方向性成立**，但**单靠它仍不复现脱落**；时间推进不是唯一原因。

**PISO 时间推进（本轮新增）**：`PressureSolver(piso_correctors=N)` —— 每时间步动量**只解一次**，随后做 1+N 次压力修正（后续校正不再欠松弛、每次用校正后速度重建 Rhie-Chow 面通量），默认 0 = 纯 SIMPLE（**位级零回归**）。

| 配置（h=D/4、slip、limited、n_inner=1、dt=0.1、220 步 ≈21.9 s ≈4.8 个脱落周期） | 四段振幅 | 趋势 |
| --- | --- | --- |
| PISO(2) | 0.00643 → 0.00102 → 0.00020 → 0.00008 | **0.012** |

→ **PISO 亦未复现脱落**（衰减反而更快）。至此已试：一阶上风 / 二阶受限中心 / 滑移壁 / 8 单元每直径 / n_inner=1 / Re=1000 / PISO(2) —— **七组配置全部单调衰减**。

**Re 判别实验（h=D/4、slip、limited、n_inner=1、dt=0.05、200 步 ≈10 s，ν=2e-6 → Re=1000）**：三段振幅 0.0040 → 0.0032 → 0.0004，趋势 **0.098** —— **把 Re 提到 5 倍仍不脱落**，据此**排除"分辨率/有效 Re 不足"这一解释**，指向更根本的数值耗散（压力-速度耦合与时间推进的隐式平滑共同抬高了离散系统的失稳阈值：物理上 Re=200 对称解已不稳定，而本离散在 Re≈1000 仍稳定）。

**第二轮：时间步假设的 A/B 检验（本轮实测）** —— 两个配置都施加**同一个非对称初扰动**
（圆柱上方 0.6D 处、宽 0.5D 的高斯竖直速度团，幅值 0.05·U；`official_diff.run_case(perturb=…)` 新增 API），
网格 12,216 单元（h=D/4 阶梯、**单层准二维** `thickness_D=0.25` + 滑移壁，成本减半），`central` + `PISO(1)` + `n_inner=1`：

| 配置 | dt | 步数 / 物理时长 | 分窗半峰振幅 | 结论 |
| --- | --- | --- | --- | --- |
| A：旧默认步长 | 0.16 s（0.2·D/U） | 600 步 / 95.4 s = **119 D/U** | 0.00371 → 1.6e-4 → 3e-5 → 1e-5 → 1e-5 → 0 | 扰动**完全衰减**；且时长远超所需（119 个对流时间单位） |
| B：步长缩小 4× | 0.04 s（0.05·D/U） | 450 步 / 19.0 s = 24 D/U（按结论收尾） | 0.00446 → 0.00132 → 3.1e-4 → 1.4e-4 → 3e-5 | **仍单调衰减**，物理时间上并不更慢 |

→ **时间步假设被证伪**：dt 缩小 4 倍不改变"扰动衰减"的定性行为；A 组覆盖 119 D/U（远超文献 Re=200 起振所需的 20–40 D/U）同样不起振。
结合此前 Re=1000 仍不脱落，**主因不在时间推进/时长/Re，而在空间离散**（h=D/4 阶梯圆柱 + 一阶时间推进的组合耗散）——这同时否掉了"算力/时间不够"的解释。
证据落盘：`_s2_A_dt016.json`（600 步完整序列）、`_s2_B_dt004.json`；两者均含 `perturb_mag` 与逐步 (t, cl)。

**第三路（已完成）：空间分辨率提到 8 单元/直径** —— h=D/8 阶梯、单层准二维（`thickness_D=0.125` → **48,840 单元**）、dt=0.04 s、1500 步 = 60 s = **75 D/U**（≈16 个脱落周期）、同一非对称扰动；
实测 1.8 s/步（直接稀疏 LU 路由）→ 全程 1500 步已跑满（75 个对流时间单位）：

| 分窗半峰振幅（每 93 采样） | 结论 |
| --- | --- |
| 4.21e-3 → 5.2e-4 → 1.6e-4 → 7.1e-5 → 3.7e-5 → 2.1e-5 → 1.3e-5 → **9e-6** | 单调衰减；后半段/前半段 = **0.0086** → 收敛到对称定常解，**仍未脱落**（报告里的 St=0.1099 是衰减暂态的 FFT 假峰，振幅比 0.015，判定未通过） |

→ **阶梯表示本身（块状圆柱 + 8 单元/直径 + 一阶时间推进）的组合耗散足以压掉 Re=200 的不稳定性**：把网格从 4 加密到 8 单元/直径、把 dt 缩小 4 倍、换格式、加滑移、加 PISO、加非对称扰动，全部无效。
→ 这一路同时服务 S4：48,840 单元 1.8 s/步 **达标**，而 97,680 单元 ≈10 s/步 **差 5×**（见 S4 表）。

**第四路（本轮）：贴体 O 型网格** —— S4 先修掉了 O 型网格的协调性缺陷（棱柱→tet 对角线未按全局编号规范化，见 S4 节），首次把**非阶梯**贴体网格接进圆柱算例：

| 配置 | 结果 |
| --- | --- |
| dt=0.04 s（θ 向间距 1.3 mm → CFL≈1.5） | 40 步内 cl 由 1.5 爆到 1e32 —— **发散** |
| dt=0.01 s（CFL=0.19） | 仍 40 步内发散（振幅 3.6e3、平均升力 429） |
| 四格式对照：upwind / limited / central / upwind+无扰动 | **全部发散**（振幅 1.5e3–6.4e3） |

→ **排除** CFL、对流格式、初扰动；定量根因由新诊断 `mesh_quality.orthogonality_report()` 给出：O 型网格内部面非正交角 median **56.6°** / p95 **81.9°** / max 86.3°，偏斜 p95 **0.97** / max 1.25（对照阶梯网格 median **0°** / p95 35.3°、偏斜 0.289）；稳态对照同样发散（残差 0.009→0.386、|u|max 8.4e8）vs 阶梯收敛（1.9e-6、|u|max 0.07）。
**第 2 步后的更新（本轮）**：S4 已把**限幅非正交修正**（`corr_limit=0.33`）接入求解器，贴体 O 型网格**稳态**从发散变为完全收敛（残差 5.4e-6、|u|max 0.069）→ 贴体路线的**稳态**阻断已解除；**瞬态**仍未用新开关重试（下一轮的 S2 实验）。

**诚实结论**：贴体路线的阻断点是**网格正交性 + 求解器的非正交/偏斜修正能力**，不是连通性、也不是时间推进。要复现脱落需先做正交性受控的贴体网格（角部椭圆平滑/O 型改造），或给求解器加 over-relaxed 非正交修正 + 偏斜修正 —— 本轮两项都未完成，故 S2 仍无 St。

**下一步（按性价比，已量化）**：
1. **空间离散改造（升为第一优先）**：贴体（非阶梯）近壁网格 ≥20 单元/直径（`mesh_prism` 棱柱层入环 + O 型网格细分辨率下的矩阵奇异性修复），或把 S1 官方桥用于**同网格官方对照**（B 路线）；`n_inner=1` + PISO 式双压力修正 + 更小 dt（需先做 ④ 性能，否则单次实验 ~10 分钟）；
2. **性能（仍为前置）**：48,840 单元 1.8 s/步、97,680 单元 ≈10 s/步（超线性，见 S4 表）→ 20 单元/直径（≈30 万单元）单组参数扫描仍不可行，需 S4 的架构级改造（编译内核 / 并行 AMG / 多重网格）；
3. **官方参考对照**：用 S1 的官方桥在同网格上跑官方求解器（B 路线），把"我们的实现差在哪"从推断变成对照。
**诚实边界**：本轮**没有**得到 St 数值，也**没有**证据表明换格式/换壁面条件即可复现；不得据此宣称对标达成。

**第五路（本轮）：方形 O 型环带 + 张量积外围混合网格（③(a) 落地，新模块 `mesh_hybrid.py`）** —— 把“求解器友好的环带”与“近笛卡尔外围”**协调**拼接：

- **构造（零浮点失配的拼接）**：环带外边界 = 与圆柱同心的正方形（2a = 6D），四边落在张量网格线上；4m = 96 条射线（θ = 45° + i·90°/m，对角方向恰为射线 → 正方形四角是环带顶点）从圆柱面**直线**连到正方形边，落点与边细分点满足解析恒等式 a·cot(45°+iΔ) = a·tan(−45°+(m−i)Δ)，实现上**直接共用同一数组**（端点显式置 ±a，消掉 tan(45°) 的 1 ulp 误差）；外围 9 块张量积块（左右带/上下带/四角）共用线数组。`mesh_conformity` 全过：**0 重复面 / 0 开边 / 0 悬挂节点**。
- **tet 分解的协调性**：2D 四边形按短对角线切 2 三角形（对角线只在本四边形内部，不跨单元）→ z 拉伸成三棱柱 → 全局编号规范化 3-tet 分解（与 `channel_tet_mesh` 的 S4 修复同款：侧面四边形对角线取编号较小的底面顶点，共享面两侧算出同一条）。
- **解析核对**：体积相对误差**全部**来自圆柱内接 96 边形（可解析预测：实测 4.4e-6 ≈ 预测值），拼接界面本身精确 —— 测试断言体积与内接多边形预测一致到 1e-9。
- **质量校准**（同一 `orthogonality_report` 口径 + 杀手指标 d_n/|d|<0.15 占比）：

  | 网格 | median | p95 | skew p95 | d_n/|d|<0.15 | 判定 |
  | --- | --- | --- | --- | --- | --- |
  | 阶梯 h=D/4（已知稳） | 0° | 35.3° | 0.289 | 0% | good |
  | O 型 96×14（已知瞬态发散） | 40.6° | 76.2° | 0.528 | 2.36% | marginal |
  | **混合默认（m=24, n_r=16, s=1.3, T=0.25D, 19,020 tet）** | **32.6°** | **69.0°** | **0.350** | **0%** | marginal |

- **z 长细比杠杆（负结果记录）**：thickness_D=0.5 时近壁棱柱（面内 ~1 mm × z 20 mm）的 3-tet 内剖面对角面非正交高达 ~84°（p95 82.8° poor）；thickness_D=0.25（z ≈ 外围 h）→ p95 69°。tet 分解对 z 长细比敏感是**本质的**：高细比单元的任何 tet 剖分都会产生近平行于长轴的内剖面。外围 h=D/8 + T=0.5D 同样 poor（61.9°/p95 74.5°）—— z 厚必须匹配外围 h。
- **稳态实测**（19,020 tet，limited 对流，80 步）：经典发散（残差 8.5e-6→37、|u|max 8e12）；**nonorth corr=0.33 收敛**（残差 4.9e-5）；**skew_corrected 在该网格上压力矩阵奇异**（dgstrf info 1/2 → NaN，负结果，默认保持关）。更重要的信号：**“稳态”解拒绝收敛到对称定常** —— cl 在 ±0.04 振荡换号、|u|max 在 0.08–0.54 间徘徊、残差停在 1e-5 量级不再下降。对称定常解在 Re=200 本就是不稳定不动点，这正是脱落存在的数值前兆。
- **接线**：`run_case(mesher="hybrid", nonorth_corrected=True, corr_limit=0.33, hybrid_m/nr/a_D/stretch/layers=...)` + CLI `--mesher hybrid --nonorth-correct --corr-limit 0.33 --hybrid-*`；结果带 `mesh_quality`（verdict/median/p95/skew）与 `w_absmax_final`（准二维真实性监视：tet 分解 z 不对称注入的伪 w，滑移壁下无阻尼 —— 如实记录）。测试 `tests/test_mesh_hybrid.py` 8 项全过（协调性/解析体积/质量判定/长细比杠杆/圆柱面识别/求解器冒烟/run_case 接线/报告助手）。
- **瞬态实测（两轮完整 + 一轮内迭代对照，全部 19,020 tet / dt=0.01 s / PISO(1) / 滑移壁 / 扰动 0.2 / nonorth corr=0.33）**：

  | run | 对流格式 | n_inner | 步数/时长 | cl 终值 | 高通振幅 | 过零（后半段） | 结论 |
  | --- | --- | --- | --- | --- | --- | --- | --- |
  | 1 | limited | 1 | 1525 步 / 15.2 s（主动终止换对照） | 0.0087 | 7e-6 | 1 | **贴体瞬态首次稳定**；扰动先衰减后慢速再生长，饱和到定常非对称 |
  | 2 | central | 1 | 4000 步 / 40 s = 50 D/U | 0.0612 | 7e-6 | 1 | 生长强 5×，仍饱和到定常非对称；St=0.04 是漂移假峰 |
  | 3（对照） | central | 3 | 300 步 / 3.0 s | （增长中 0.0188） | — | — | **同物理时刻 cl 是 n_inner=1 的 3–4×**（t=2.7 s：0.0188 vs 0.0079） |

  **诊断（本轮最重要的机理发现）**：延迟修正格式下隐式矩阵恒为上风，高阶差值只是显式源 —— `n_inner=1`（每步一次 SIMPLE）时**有效对流精度仍接近一阶上风**，尾迹里的振荡模态被数值耗散压掉（run1/2 高通振幅 7e-6、无一次换向过零）。`n_inner=3` 让高阶源在步内收敛，扰动生长率立刻 ×3–4 → **内迭代次数是“格式阶数”的实际开关**，不是单纯的收敛性参数。
  **与阶梯网格对照的实质进步**：阶梯（cartesian）全部配置扰动**单调衰减**（第三路 h=D/8 跑满 75 D/U 仍衰减到 9e-6）；混合网格扰动**持续生长**（不衰减）且瞬态全程无发散 —— 环带贴体 + 非正交修正把流场推到了“接近 Hopf 阈值但未越过”的位置（定常非对称分支 = 对称定常失稳后的第一个分支，物理上就是 Hopf 前兆）。
- **run4（已完成）**：central + **n_inner=3** + 1500 步（t=15 s）：扰动生长比 n_inner=1 强 3–4×（t=2.7 s：0.0188 vs 0.0079），cl 终值 0.0544；但增长率持续减速（σ 0.23→0.059/s）→ **仍饱和到定常非对称分支**，振荡模态全程被阻尼（高通 7e-6、过零 1）→ 步内收敛把有效 Re 提高，仍未越过 Hopf 阈值。证据：`_s2_hybrid_ckpt4.json`；已录入 `benchmarks/ours_cylinder.json`（cyl_re200_hybrid_central_ni3）。
- **run5（corr_limit 0.6 对照，300 步探针）**：同时刻 cl 反而低于 0.33（t=3.0 s：0.0156 vs 0.0188）、无振荡 → **过度修正不增益，corr_limit 旋钮打满**。证据：`_s2_hybrid_ckpt5.json`；已录入 benchmark（cyl_re200_hybrid_corr06）。
- **第六步（本轮末）：尾迹渐变加密 + planar_2d —— 两个都是负结果，已回退**：
  ① `mesh_hybrid._graded_line`：外围几何渐变（近正方形 ≈h、远场放粗 ≤h·stretch_far），h=D/8 时 17,472 单元（比均匀 D/4 少 8%）且协调性/解析体积/正交性统计全保持 —— 但**瞬态立即发散**（stretch_far=4 与 2 都炸，cl 首窗 0.12-0.27、w 飙到 1.45 = 29U）：近尾迹 h=0.005 使 z 长细比 ρ=2，tet 内剖面的伪 w 模态失稳。**结构性限制**：单 z 层设计中 z 全局恒定，面内尺寸不能变化 4×（均匀 h=D/4 的 ρ≈0.6-1.25 恰是甜点）。
  ② `PressureSolver(planar_2d=True)`（强制 w≡0、跳过 w 动量）：**与渐变网格叠加时发散**。机理：tet 内部斜面法向含 z 分量，去掉 z 动量方程后 Rhie-Chow 通量模型对这些面失去平衡 → 压力泊松被喂入不一致通量。三维 tet 求解器不能这样干净退化到二维（负结果，默认 False 零回归，测试保留 w≡0 的机制性验证）。
  （修复记录：渐变线曾漏加 xR/yT 偏移 → 1584 重复单元，bbox 越界暴露；w 压力修正曾绕过清零 —— 均由测试抓住。）

- **run9（本轮最终实验）**：均匀 h=D/4（已证稳定配置）+ central + ni=3 + 3000 步目标 —— 与 run4 逐步一致（**确定性复现验证**），外部终止于 1125 步/t=11.25s（无 stderr、JSON 空文件 = Windows 强杀，非求解器崩溃）；cl=0.048 仍按 σ≈0.058/s 减速生长、无振荡（过零 1）→ 与 run2（ni=1 饱和 0.061 @t=40）同一模式：**ni=3 只改变生长速度，不改变饱和归宿**。证据：_s2_hybrid_ckpt9.json；已录入 benchmark（cyl_re200_hybrid_ni3_3000）。
- **第七步（本轮）：两个未测杠杆的对照 —— 都是负结果**：
  ① **欠松弛因子**（`alpha_momentum=1.0, alpha_pressure=1.0`，瞬态 PISO 的教科书设置）：**第 1 步即发散**（cl=-135.8、|w|=1.37）→ 欠松弛在本求解器上不是可选加速项而是**稳定性必需**（分离式 SIMPLE + 一阶时间推进的组合需要 α_m=0.7 / α_p=0.3）；
  ② **全域温和加密**（h=D/5、m=28、n_r=18，26,352 单元，ρ=1.25 仍在稳定区间）：300 步同时刻 cl **略低于**基线（t=3.0 s：0.01725 vs 0.0188，−8%）→ 该量级加密**不加速**扰动生长，与"阻断不是本量级分辨率"的判断一致。
  ③ **薄展向对照**（T=0.125D、z=0.005，m=32/n_r=20 —— 早前质量扫描正交性最优组合，27,612 单元）：**定性反转** —— cl 在 t=2.0 达 0.00186 后**衰减**到 t=3.0 的 0.00139（同时刻厚板基线 0.01293，低 7×），伪 w 也更低（0.0034 vs 0.0045）。**假设性解释（未证）**：z 越薄伪 w 被 z 向粘性阻尼越强 ⇒ 此前"非对称持续生长"**很可能部分是 z 不对称数值伪影**；扣除伪影后底层离散仍偏耗散（有效 Re < Hopf 阈值），正确渐近就是对称定常。该假设同时解释了阶梯网格（更小 z 厚）"扰动必衰减"的行为，与"阻断=离散耗散"的主结论一致。
  证据：`_s2_p7A.json` / `_s2_p7B.json` / `_s2_p7C.json`；已录入 benchmark（cyl_re200_hybrid_refine_D5 / cyl_re200_hybrid_noURF / cyl_re200_hybrid_thin_slab）。
- **本轮 S2 总结论（13 例诚实入库，全部未达成 St）**：混合网格路线达成三件事 —— ①贴体瞬态首次稳定（此前 O 型全发散）；②扰动从“必衰减”（阶梯）变为“持续生长”；③三个机理定位（延迟修正的内迭代开关、z 长细比-伪 w 通道、planar 退化不可行）。剩余阻断的定量刻画：有效 Re 被离散耗散压到 Hopf 阈值之下，系统饱和到**定常非对称分支**（cl≈0.06-0.08，非物理的离散对称破缺）。下一轮候选（按算力性价比）：① 环带+外围同时小幅加密到 h=D/6（ρ≤1.5 甜点内，~24k 单元，温和）；② 2 层 z + 每层真棱柱 FVM（内核级改造）；③ S1 官方桥做同网格官方对照，把“差在哪”变成对照数据。
- **第五路诚实结论**：贴体混合网格把流场从“扰动必衰减”（阶梯）推到“扰动持续生长但饱和到定常非对称”（环带+非正交修正+步内收敛二阶对流）；仍未得到 St，S2 维持未达成。本轮已排除的杠杆：对流格式（limited→central 显著增益但 central 后到顶）、内迭代（1→3 显著增益后饱和）、修正限幅（0.33 最优、0.6 负增益）、尾迹加密（h=D/8 因 z 长细比失稳，见第六步）。



### S2 原始目标（存档）
- **目标**：官方同工况（U=0.05、D=0.04、ν=1e-5、Re=200）下复现涡脱：St 落入 0.176±15%、升力振幅比落入 0.3–3.0；否则如实记录并给出下一步。
- **手段（按性价比）**：① 对流格式升级（fvm_core 已有 limiter/_barth_ratio，pressure_solver 现走上风 → 增可选二阶/中心+限制器，默认不变零回归）；② 近壁加密（mesh_prism.prism_layers 已有，接 run_case）；③ 局部加密（mesh_amr 钩子接入运行环）；④ 时间推进（自适应 dt / ≥10 周期）。
- **验收**：新增 tests/test_shedding.py（fast：稳定性与格式对比；long：St 带内）；R1 回归与 P4–P12 全量不回归。
- **诚实边界**：四项手段仍不达标 → 明确记录"当前内核在 Re=200 无法复现涡脱"并给出算力/格式结论，不调参造假。

### S3 解析字段级语义（P1）—— ⚠️ 第二轮达成（解场根节点 + 二维网格语义 + 几何字段假通过修复；T 串归属与校验和仍开放）

**本轮交付（真缺口 + 真修复）**：
- **缺口**：`extract_solution_fields` 原先只认 `star.post.SolutionRepresentation`；而**官方新求解并保存**的 `.sim` 不创建该对象 —— 解场直接挂在 `star.common.FvRepresentation` 的 cells DUP 组上。上一轮官方桥生成的 `airfoil_official_2000.sim` 因此 `ok=False`（诚实拒绝，但能力缺失）。
- **修复**：根节点泛化为两类（SolutionRepresentation 优先，缺失时回退 FvRepresentation），下游链路（Objects → TypedObjectManager → FvRegionManager → FvRegion.cells DUP → map 字段）不变。
- **实测**：官方生成翼型件现在抽出 **21 个字段 / 16987 单元 / region=Domain**（Pressure −3230~1121 Pa、Density 1.143–1.186 可压缩理想气体、EffectiveViscosity 2.09e-5~6.59e-2、PrismLayerCells、Solidity…）；`v3_0.05` 对照**结果不变**（10 字段 / 20245 单元）→ 零回归；未求解文件仍诚实拒绝（"解场 FvRegion 无字段存储"）。
- `tests/test_solution_roots.py` **3 项**：生成件走 FvRepresentation 根（且断言其确实不含 SolutionRepresentation）、教程件走 SolutionRepresentation 根（回归）、未求解件诚实拒绝。

**第二轮交付（2026-09-15，二维网格语义 + 一处假通过修复）**：
- **发现 1（二维网格被当成三维）**：`vortexShed_tutor_v3_0.05_2502.sim` 实为**二维**案例，但 .sim 同样用「面-单元」存储体系：Coord 40044 顶点（**z 恒为 0**）、VertexList 每"面"恰 2 顶点（59811 条**边**、119622 个顶点引用）、FaceCellIndex 2×59811、20245 个单元。旧实现只按三维多面体解释 → 维度谎报为 3D，导出 VTK 时写出**退化的 2 顶点"面"**的 VTK_POLYHEDRON。
- **修复 1**：`mesh_dimensionality()` 判常量轴 → `extract_volume_mesh` 现在返回 `dim/planar/planar_axes/extents/face_kind`（`face_kind="edge"` 表示每面 2 顶点）；`export_volume_vtu` 在二维且环闭合时改导 **VTK_POLYGON(7)**。实测 v3_0.05：dim=2、常量轴 z、20245 个多边形单元、connectivity 120096、offsets 累计一致、XML 可解析，文件 3.34→2.36 MB。
- **发现 2（边界边不在主面组）**：实测边界 patch 组共 **478** 条边，与主面组边集**交集为 0** → 贴壁单元在主面组里的边集是"**缺一条边的路径**"（474 个单元：4/5/6 条边的路径形态）。VTK 多边形按定义隐式闭合顶点环，故 `assemble_planar_cell_loops()` 支持路径形态：v3_0.05 得到 **19771 显式闭合 + 474 隐式闭合 + 0 失败**；其中 **470** 个隐式闭合边可在边界组中找到（=真实边界边，几何精确），其余 4 个是只有 2 条存储边的退化三角形。
- **发现 3（假通过）**：根节点回退 `FvRepresentation` 后，**未求解**文件 `pipeBlockage.sim` 被抽出 2 个字段（`CellGeometryPartIndex`、`ProstarCellIndex`）并返回 `ok=True` —— 几何索引字段被当成了"解"。
- **修复 2**：新增 `GEOMETRY_ONLY_FIELD_RE`；**全部**字段命中几何/索引模式才判"无解场数据"（未知物理量仍按解场处理，避免误拒绝）。现在 pipeBlockage 诚实拒绝：`ok=False`、`fields/data` 置空、`geometry_only_fields` 另列、reason="无解场数据（仅几何索引字段：…）"；官方生成翼型件（Pressure/Density/EffectiveViscosity）与 v3_0.05 教程件**零影响**，成功返回另附 `solution_fields`。
- **测试**：新增 `tests/test_mesh_2d.py` **12 项**（离线：维度判定 3 类、环/路径/分叉/多路径/多重图/空单元 6 项；语料：二维识别、20245 环完整性、边界组交集为 0 与 470/474 精确计数、VTU 全 7 型 + offsets 累计、v3_0.025 诚实拒绝、**三维语料不被二维路径劫持**（methaneOnPt dim=3/types=41））；`tests/test_solution_roots.py` 增至 **4 项**（新增几何字段≠解场）。

**仍开放（诚实标注）**：① T 载荷**字段级归属**（双精度串 39.5% / u16 串 42% 仍未绑定到对象/属性）；② 数组字段级余量（121 块）；③ 状态表尾部校验和；④ `v3_0.025` 体网格抽取缺口 —— 本轮已把证据钉死：其 FaceCellIndex 只引用 **2780/20245** 个单元（每单元重复 ~47 次）、VertexList 下标上限 5398，且**整个 28 MB 文件按 u4 四种对齐扫描都找不到单元下标 20244**（v3_0.05 找得到）；数组载荷偏移经逐字节比对无误（每个数组载荷末尾恰好紧接下一个数组头，布局自洽），int64 重解释给出 1e13 量级荒谬值 → 既不是"读错块"也不是"int64 对编码"，而是该文件把网格索引存成了**部分值域/共享存储**形态。在拿到该编码规则前保持 `ok=False + "拓扑反演失败：17465/20245 单元无面"`，**不编造拓扑**（已加断言）。
### S3 原始目标（存档）
- **目标**：把 T 载荷的双精度串 39.5% / u16 串 42% 从"结构可分解"推进到"可归属"（绑定到对象/属性），并补齐数组字段级（余 121 块）。
- **手段**：① 对象图数值/引用集合做假设检验框架（命中率 + 反例）；② 数组标注加规模自洽 + 引用图双证据；③ 状态表尾部校验和（用官方重存对照）；④ NameManager 存储定位。
- **验收**：T 串字段级归属率 ≥60%（带反例证伪）；数组标注 ≥95%；尾部记录语义定论；v3_0.025 体网格抽取修复（benchmark 文件普查）。
- **诚实边界**：归属结论必须附"证据类型 + 反例数"，不得只报命中率。

### S4 网格-求解联调与性能（P1，S2/S6 的前置）—— ⚠️ 第三轮达成（AMR 协调入环 + O 型网格协调性缺陷修复 + 正交性诊断；10 万单元 <2 s 仍差 ~5×）

**发现并修复的真缺陷**：`_solve_scipy` 的 ILU 快路径把 `spilu` 返回对象直接当 `M=` 传给 `bicgstab`，新版 SciPy 抛 `TypeError: type not understood`，被 `except Exception: pass` 静默吞掉 → **每次线性求解都白做一次 ILU 因式分解再回退直接解**（24,432 未知量实测 1.2 s/步纯浪费）。修复：包 `LinearOperator(ilu.solve)` + `rtol/atol` 兼容旧版 `tol`。

**优化项**：
1. **按规模选路**：`n ≤ STARDECODING_DIRECT_MAX`（默认 15 万）→ 直接稀疏 LU；更大规模 → AMG（pyamg，本轮已安装）→ ILU+bicgstab → 直接解兜底；
2. **装配向量化**：边界 RHS 两处 Python 逐面循环 → `np.bincount`；`_deferred_convection_source` 与 `_check_residual` 的 `np.add.at` → `np.bincount`；
3. 线性求解选型基准（8 个真实系统，24,432 未知量）：直接 **1.44 s**（1.44s/8）| AMG 2.16 s | ILU+bicgstab 4.14 s（6/8 收敛 0.167 s、2/8 停滞 1.4–1.7 s）→ **当前规模直接解最优**，AMG 留给更大规模。

**实测（`convection="limited"` + `wall_slip_axes=(1,2)`，n_inner=2 → 每步 8 次线性求解）**：

| 规模 | 优化前 | 优化后 | 提速 |
| --- | --- | --- | --- |
| 24,432 单元 | 3.80–4.01 s/步 | **2.46–2.83 s/步** | 1.4–1.5× |
| 45,456 单元（h=D/8） | 14.0 s/步 | **5.77 s/步** | **2.4×** |
| 100,000 单元（线性外推） | ~34 s/步 | **~11.6 s/步** | — |

**第二轮实测（8 单元/直径阶梯圆柱，`central` + 滑移 + `piso=1` + `n_inner=1`，单层准二维；CLI 总耗时扣掉约 12 s 官方参考抽取，6–10 步样本）**：

| 规模 | 路由 | 实测 | 对照 S4 目标 <2 s |
| --- | --- | --- | --- |
| 48,840 单元（h=D/8，单层） | 直接稀疏 LU（默认路由） | **1.8 s/步** | ✅ 达标（但规模仅为目标的一半） |
| 48,840 单元（同上） | AMG（`STARDECODING_DIRECT_MAX=20000`） | **2.8 s/步** | ❌ 慢 1.5×：此规模直接解仍占优 |
| 97,680 单元（h=D/8，两层） | 直接稀疏 LU | **10.0 s/步** | ❌ 差 5.0× |
| 97,680 单元（同上） | AMG（`DIRECT_MAX=20000`） | **9.5 s/步** | ❌ 差 4.8×（AMG 在 10 万级首次略胜直接解） |

→ 规模翻倍耗时涨 5.5×（超线性），**瓶颈是压力泊松/动量线性求解本身**；<2 s 需要架构级改造（编译内核 / 真正并行的 AMG / 多重网格），已如实记录为未达成。

**验收对照**：目标"10 万单元单步 <2 s"**未达成**（外推 ~11.6 s/步，差 5.8×）。进一步手段（已记录，未实施）：AMG 在大规模上的调优（本轮已装 pyamg，但 >15 万规模未实测）、降低内迭代次数（`n_inner` 属物理选择，见 S2）、混合精度/编译内核（numba/Cython，属架构级改造）。
**测试**：新增 `tests/test_linear_solve.py` **5 项**（直接解路由精度 / 环境变量强制迭代路径仍准确 / auto-numpy 一致 / 残差检查向量化正确 / RHS 形状守门）；求解器回归 **75 passed**（convection + pressure_solver + solver_run）零回归。
**AMR 入环（第二轮落地，S4 剩余项之一）**：
- **发现（正确性缺陷）**：原 AMR 路径在 `run_amr` 返回新网格后直接 `solver.set_mesh(...)` + `_initialize_field()` → **把已发展的解清零**；且 `refine_tets` 是**非协调**红细化（邻接未细化单元处产生悬挂节点），而 FVM 按顶点集合配对单元面 → 悬挂界面被误判为边界、内部连通丢失。
- **落地 API**：`mesh_amr.refine_conformity(mask, cells)`（红细化协调性**精确前置判据**：不存在"一侧细化、一侧不细化"的共享面 + 无 >2 共享面，O(面数)）；`mesh_amr.mesh_conformity()`（结构诊断：重复面 / count==1 面的开口边 / **T 型节点**——某条边的内部存在被同一批面使用的其它顶点）；`refine_tets_conforming()`（边闭包迭代至多 `max_rounds` 轮 → 1:8 红细化 → 双门槛复核，返回**父子映射** `parent` 与 `n_closed/rounds/conformity`）；`transfer_fields(parent, values)`（注入式传场：常数场逐点精确、体积加权均值保持——1:8 的子单元恰好填满父单元）；`PressureSolver.set_fields(u,v,w,p,snapshot=)`；`SolverBackend.set_amr(interval, threshold, max_rounds)` 走上述链路并记录 `transferred / skipped / note / error`。
- **诚实边界**：判据不过（非协调，或输入网格本身结构异常，例如仓库里的 5-tet `demo_mesh` 扇形基元）→ **跳过**本次细化并记 `skipped` 原因，不把非协调网格塞进求解器；节点场求解器（`DemoDiffusionSolver.field()` 是顶点量）无法按单元父子映射传递 → 保持"换网格 + 重初始化"的旧行为并在 `note` 里写明，不假装传过。
- **测试**：新增 `tests/test_amr_loop.py` **7 项**（Kuhn 网格判协调并核对边界面数；5-tet `demo_mesh` 被结构诊断判非协调；单单元细化被精确前置判据拒绝；边闭包后双门槛通过 + 体积守恒 + 父子映射计数 ∈{1,8}；场传递常数精确/体积加权均值保持；**端到端：AMR 后场与原样传递逐位一致且 std>0**（旧实现会跳回均匀初值，std≈0）；两条诚实拒绝路径）。`tests/test_solver_run.py` 原 AMR 用例改为协调网格 + 新契约（钩子注册表仍保留为公共工具 `run_amr`）。
- **prism 入环仍未做**（S4 剩余项）：内核 `mesh_prism` 已具备但未接运行环，保持诚实标注。

**O 型（贴体）网格：协调性缺陷修复 + 可用性定量判据（本轮实测）**
- **发现并修复的真缺陷**：`channel_tet_mesh` 的棱柱→3 tet 分解**按局部顶点次序**取侧面对角线。θ 周期缝合面上同一四边形两侧分别来自"第 1 个三角形"与"第 2 个三角形"，局部次序相反 → 一侧取 `bottom(i,0)–top(i+1,0)`、另一侧取 `bottom(i+1,0)–top(i,0)` → 内部面配不上：**n_theta=48 实测 1348 个内部单侧面**（不在任何域边界/圆柱面上的 count-1 面）→ 细网格压力矩阵奇异（历史记录的 `dgstrf info` / NaN 根因）。
- **修复**：按**全局顶点编号**规范化底面三角形顺序（顶面同序跟随），使对角线只由全局编号决定；docstring 同步澄清"外边界是内接多边形（角部切弦）→ 体积略小于解析值"。
- **修复后实测**：n_theta/n_r = 24×4 / 48×7 / 96×14 / 192×28 全部 `conforming=True`（开口边 0、重复面 0、T 型 0），边界面数精确 = 2n_θ(2+2n_r)（48×7 → 1536、96×14 → 5760 ✓）；细网格求解器可稳定推进：32,256 单元 1.75 s/步、55,296 单元 3.29 s/步（修复前这些规模直接奇异）。
- **但贴体网格仍不可用于 S2（诚实结论）**：新增诊断能力 `mesh_quality.orthogonality_report()`（内部面非正交角 + 偏斜 + `good/marginal/poor` 判定）给出定量根因——**O 型网格 median 56.6° / p95 81.9° / max 86.3°，偏斜 p95 0.97 / max 1.25 → poor**；对照阶梯网格 **median 0° / p95 35.3°、偏斜 0.289 → good**。求解器对照：O 型**稳态** 60 步残差 0.009→0.386、|u|max 8.4e8（发散），阶梯稳态残差 1.9e-6、|u|max 0.07（收敛）；O 型**瞬态** 40 步在 upwind/limited/central（含无扰动）四种组合下全部发散，且 dt=0.01 s（CFL=0.19）仍发散 → **排除 CFL、格式、扰动，阻断点是网格正交性 + 本求解器非正交修正能力**。要打通贴体路线需：正交性受控的网格生成（各向异性椭圆平滑/O 型角部处理）或求解器的 over-relaxed 非正交修正 + 偏斜修正。
- **CFL 诊断入环**：`run_case` 返回并打印 `h_min/h_typ/cfl_max`（`h_min` 取最小单元体积的立方根）——本次正是靠它发现 θ 向间距仅 1.3 mm、dt=0.04 s 已 CFL≈1.5。
- **测试**：`tests/test_official_diff.py` +3（协调性/缝合面共享回归守卫、细网格求解稳定、O 型 `poor` 与阶梯 `good` 对照）；`tests/test_mesh_quality_n6.py` +3（Cartesian `good`、剪切网格 `marginal`、空输入诚实拒绝）。

**第 2 步：偏斜 / 非正交修正接入求解器（本轮）**
- 新开关（**默认关 → 零回归**）：`PressureSolver(skew_corrected=, nonorth_corrected=, corr_limit=)`；`corr_limit=0` 与经典路径**逐位一致**（测试锁定）。
- **配对辨析（本轮关键实测）**：经典扩散系数 A/d_n 属 Jasak **过松弛**分解，配套修正向量是 **k_or = n̂ − d/d_n**；而最小修正分解（隐式 A/|d| + k = n̂ − ê）虽面级也精确，作隐式系数会让近切向面失去对角占优（实测发散）。面级检验（线性场 + 精确梯度）：经典 2.5e-1（O 型 1.75）→ 加 k_or 后 **1.4e-15（O 型 9e-15）**。
- **验收（O 型 48×7，40 步稳态）**：经典 |u|max **1.75**、残差 0.18（失稳）；`nonorth_corrected=True, corr_limit=0.33` → 残差 **5.4e-6**、|u|max **0.069**（与入口 0.05 同量级 = 物理）；`skew_corrected + 0.33` → 残差 1.3e-5、|u|max 0.075。**贴体 O 型网格首次稳态可解**。
- **限幅是必需的**：同网格 `corr_limit=1.0` 过冲失稳（残差 0.171、|u|max 3.6e3）；在*中度*非正交的极坐标环带上经典本就收敛（3.4e-5），全量修正反而变差（0.126）。→ 口径：**经典/笛卡尔网格保持默认关**；**强非正交贴体网格用 corr_limit=0.33**。
- 配套接线：`_grad()`（LSQ → Gauss(recon) 两遍迭代）与 `_face_value(..., grad=)`（偏斜修正）已用于压力梯度、面速度与 Rhie-Chow；压力修正的延迟修正与通量更新使用**同一 p′ 参考**（否则首步残差被放大 ~230 倍：0.006 → 1.4）。
- 测试：`tests/test_pressure_solver.py` +3（`corr_limit=0` 位级 no-op、O 型失稳 ↔ 限幅修正收敛、偏斜修正下 Cartesian 仍收敛）。
- **瞬态仍未通（本轮实测，诚实标注）**：用新开关重试 O 型 96×14 的瞬态（dt=0.01、central+PISO(1)+扰动、80 步）：经典 |u|max 4e15、限幅修正 540、偏斜+修正 2e59 —— 修正**延缓但不能阻止**瞬态发散。故 S2 的贴体瞬态路线仍被阻断；下一步要么做**隐式**非正交处理（当前是显式延迟修正），要么改网格（正交性受控生成 / 极坐标环带+阶梯 Cartesian 混合——环带已验证经典即可收敛）。
**第 3 步：边界闭环（Neumann 样本只约束法向）**
- **缺口**：`grad_lsq` 的零梯度（Neumann）边界样本用 (r_b, Δφ=0)，表达的是"沿 r_b 方向导数为零"，只有 r_b ∥ n̂ 时才等价于真实边界条件 ∇φ·n̂ = 0；r_b 的切向分量把"切向导数为零"的错误信息塞进 LSQ，污染近壁 1–2 层梯度（实测边界/邻壁 1.8–2.9，深层 1e-14）。
- **修复**：把零梯度样本的 r_b **投影到面法向**（Dirichlet 样本不变）。
- **实测（判别场 φ = cosπx + cosπy + cosπz：六壁面法向导数 = 0、切向导数 ≠ 0）**：近壁梯度误差 Cartesian **1.87 → 1.24（0.66×）**、剪切 0.6 网格 **3.19 → 2.92（0.91×）**；深层内部逐位不变。
- **测试**：`tests/test_fvm_core.py` +1（判别场 old/new 对比 + 深层不变）。
- **对贴体网格的影响**：稳态不变（残差 4.1e-3 / 9.3e-4），瞬态仍发散 → 该修正是必要的"补课"，但不是瞬态阻断的解。

**瞬态贴体阻断：本轮穷举后的结论（诚实标注）**

O 型 96×14 瞬态 80 步（dt=0.01、PISO(1)、同一非对称扰动）逐项排除：

| 变量 | 取值 | \|u\|max 结果 |
| --- | --- | --- |
| 对流格式 | central / limited / upwind | 4e15 / 245 / 182（**都发散**） |
| 非正交修正 | 关 / `corr_limit=0.33` | 4e15 / 182（修正只延缓） |
| 偏斜修正 | 开 | 2.8e34（**更差**） |
| 压力修正内迭代 `pc_inner` | 2 / 4 | 立即 NaN（延迟项被重复计入） |
| 内迭代 `n_inner` | 1 / 2 | 都发散（2 更差） |

→ 稳态完全收敛（残差 5.4e-6）而瞬态必发散，且与格式/修正/内迭代无关 ⇒ **阻断在网格本身**（非正交 median 56.6°、偏斜 p95 0.97、6% 近切向面）。
下一步必须走**网格路线**：正交性受控的椭圆生成，或**极坐标环带 + 阶梯 Cartesian 混合**（环带已验证：经典格式稳态即可收敛，残差 3.4e-5、|u|max 0.048 ≈ 物理值）。

- **仍未做**：① 非正交项仍是**显式延迟修正**而非隐式；② 瞬态贴体路线（需先解决网格）。

**第四轮（本轮）：线性求解路线按系统类型分流 —— 10 万单元 32.6 s/步 → 5.6 s/步（5.8×）**

上一轮"按规模选路"的阈值是**误设**的：`STARDECODING_DIRECT_MAX` 默认 150000，而当年只在 **24,432** 未知量上标定过（那时直接 LU 0.22 s/次最优）。10 万级从未复标 → 一直走直接 LU（5 s/次），本轮实测把它推翻：

单次求解（97,680 未知量 / 454k nnz，同一网格同一方程组）：

| 系统 | 直接 LU | AMG 平滑聚合 | ILU(1e-4)+BiCGSTAB |
| --- | --- | --- | --- |
| 动量（对流扩散，非对称） | 5.56 s | setup 3.39 + solve 0.08 = 3.47 s | **setup 0.89 + solve 0.04 = 0.93 s** |
| 压力（泊松，对称正定） | 6.54 s | **setup 0.50 + solve 0.40 = 0.90 s** | 失效（BiCGSTAB `info=-10` breakdown） |

→ 两类系统的**最优路线相反**：动量走 ILU 预条件 BiCGSTAB（快 6×），泊松走 AMG（ILU 在对称泊松上直接崩溃）。实现为 `solve_linear(..., system="convection"|"poisson"|"auto")` + 阈值默认降到 50000（实测交叉点在 5 万附近）；迭代路线统一以相对残差 ≤ max(tol, 1e-6) 校验，不过则逐级回退（AMG/ILU → 旧路径 → 直接 LU），**绝不返回未收敛解**。

整步实测（`convection="upwind"`、`wall_slip_axes=(1,2)`、每步 4 次线性求解）：

**第二轮调优（同轮完成）**：把两条路线的参数也标定了一遍 ——

| 路线 | 参数 | setup | solve | 合计 | 相对残差 |
| --- | --- | --- | --- | --- | --- |
| 动量 ILU(1e-4, fill=10) | 高填充 | 0.93 s | 0.04 s | 0.97 s | 7.7e-08 |
| 动量 **ILU(0)**（drop=0/fill=1） | 零填充 | **0.18 s** | 0.25 s | **0.42 s** | 6.2e-07 |
| 压力 AMG + `ml.solve` | 默认 | 0.44 s | 0.40 s | 0.84 s | 7.5e-07 |
| 压力 **AMG(max_coarse=200) + CG** | SPD 走 CG | 0.15-0.35 s | **0.08-0.21 s** | **0.23-0.56 s** | 2.9e-07 |

高填充把成本全花在因式分解上（LU nnz 2.27M vs ILU(0) 的 0.39M），而 BiCGSTAB 迭代本身只要 0.25 s —— **零填充是总耗时最优**；压力矩阵对称正定，AMG 预条件 CG 比 `ml.solve` 的迭代精化快近 2×。两项都已设为默认。

**整步实测**（`convection="upwind"`、`wall_slip_axes=(1,2)`、每步 4 次线性求解；共享机器上步时方差可达数倍，故下表用 **min/median** 口径，见 `solver_bench.py` 的方差告警）：

| 网格 | 直接 LU | 仅 AMG | 分流 v1 | **分流 v2（本轮调优后）** | v2 相对直接 LU | 残差一致性 |
| --- | --- | --- | --- | --- | --- | --- |
| 97,680 单元（h=D/8, T=0.25D） | 32.64 s/步 | 12.56 s/步 | 5.60 s/步 | **min 3.28 / median 3.37 s/步**（线解 1.81 s） | **9.7×** | 1.962e-06 / 3.801e-06 / 2.275e-06，与直接 LU（1.962e-06 / 3.801e-06 / 2.277e-06）**逐位一致** |
| 48,840 单元（h=D/8, T=0.125D） | （上轮 5.77 s/步，直接 LU 已非最优） | — | 3.53 s/步 | **min 2.04 / median 2.10 s/步** | 2.7×（对第三轮最优） | 2.777e-06 / 5.395e-06 / 3.463e-06 |
| 390,720 单元（h=D/8, T=1.0D） | （未跑，直接 LU 不可行） | 53.34 s/步 | — | — | — | — |

**验收对照**："10 万单元单步 < 2 s" 仍**未达成** —— 97,680 单元 median 3.37 s（差 1.7×），但 48,840 单元已达 **2.04-2.10 s**（基本达标量级），本轮相对直接 LU 提速 **9.7×**（32.64 → 3.37）。剩余差距的量化归因：线解 1.81 s（动量 ILU(0) setup 0.18×3 + solve 0.25×3 ≈ 1.3 s；压力 AMG+CG ≈ 0.5 s）+ 组装/其它 ≈ 1.56 s。下一步候选（已量化，未实施）：① **滞后预条件子复用**（矩阵模式固定、跨步均值变化仅 2.3%，实测用旧 ILU 解新矩阵仍收敛 9.1e-07/0.50 s —— 但当前 ILU(0) 已足够便宜，需按 3-5 步复用才划算）；② 组装热点向量化（需先 profile）；③ 编译内核（Cython/numba）。

可复现产物：`solver_bench.py`（`--mode auto|direct|amg`，落盘 JSON）+ `tests/test_linear_solver_routes.py`（5 项：两条路线各自可用、分流解与直接 LU 一致、失败必须回退、阈值回归护栏）。
### S4 原始目标（存档）
- **目标**：① prism 边界层 + AMR 真正进入求解环；② 表面粗化让 R5 tet 对标可用（秒级）；③ 性能：10 万单元单步 <2 s，或明确记录当前上限与改造需求。
- **手段**：mesh_remesh 粗化 + mesh_prism 贴壁 + mesh_amr 细化；fvm_core 热点排查（面循环/稀疏装配）；线性求解器策略（CSR + AMG/spilu 双路径已有）。
- **验收**：带棱柱层的圆柱算例网格质量达标且求解稳定；R5 tet 对标 <60 s；性能基准入档。

### S5 客户端收口（P2）—— ⚠️ 第一轮达成（Mesh 两项从 NYI 桩改为诚实的会话/显示层实现；3D-CAD 与打包仍开放）

**已交付**：
- `star_gui.cmd_clear_mesh`（Mesh>Clear）：丢弃本会话生成结果（volume/poly/trimmer）+ 移除 `volume*`/`session*` 前缀 actor + 置 `document.session_meshes_cleared`；消息明示**不改对象图与 .sim**，官方网格可重新显示。
- `star_gui.cmd_convert_2d`（Mesh>Convert2D）：按视图包围盒取**最小跨度轴**压平（显示层投影）+ 置 `document.display_2d`，消息明示**对象图与 .sim 维度未改**；无 3D 视图（无头）或视图无 actor 时**如实拒绝**。
- 两动作注册从 `_kernel_nyi` 桩改为真实命令；`star_gui_parity.md` 对应行 `needs_kernel` → `session`（含边界说明）。
- `tests/test_gui_mesh_ops.py` 4 项（3 通过 + 1 无头跳过）：动作注册与桩移除、清除后的状态与提示、无视图诚实拒绝、有视图压平并置标志。

**仍开放（诚实标注）**：内核级「从网格模型删除体网格」已由第二轮解决（见下），「真正把区域转 2D」仍属 needs_kernel；3D-CAD 界内 B-Rep 建模（OCC 内核已在 C2/C3，未接 GUI）未动。
- **S5 第三轮（本轮）：真实打包达成** —— 装上 PyInstaller 6.22.3 并生成**首个可分发二进制**：`dist/stardecoding-cli.exe`（onefile，236.3 MB），冒烟用真实 `adjointWing_start.sim` 跑 `--report`，输出完整对象图/场景/Part 清单、退出码 0（`tests/test_build_exe.py` 3 项：冻结入口语法、排除巨型依赖、产物真跑；产物不入库故该项在未构建时如实 skip）。
  **过程中的真问题**：把 `scipy` 作为 `--hidden-import` 会触发 PyInstaller 的巨型 hook 图，把 sklearn/tensorflow/astropy/pyarrow 全拖进来（构建 >10 分钟、体积失控）；解析器本身**只用 numpy**（scipy 仅求解器模块需要）→ 改为只收 numpy + 显式排除 15 个重依赖。另：生成的三引号入口曾被转义写坏导致 `SyntaxError`、cp1252 控制台输出中文报错 —— 都已修并有测试护栏。
  **诚实边界**：GUI（PyQt5 + VTK）打包**未做**（依赖体积大、需额外 Qt 插件收集）；onefile 236 MB 偏大，`--onedir` + UPX 是后续优化项。
- **S5 第二轮（本轮）：Mesh>清除从 session 升级为 persist（对象图级真删除）** —— 旧实现只丢弃会话生成结果 + 显示 actor，对象图与 .sim 不变。新增三层：
  ① `sim.volume_mesh_groups()`：与 `extract_volume_mesh` 同源的角色标签集（Coord/VertexList/FaceCellIndex/ElemType/ProstarCellIndex/ProstarCellType/CellGeometryPartIndex/PrismLayerCells），但返回**全部**带拓扑角色的存储组（含重复副本 3087/3092 —— 只删一个副本时抽取仍成功，所以必须删干净）+ 网格绑定场组（SerialSize 恰等于拓扑组尺寸）+ 被引用的 SimpleStorage/ListStorage 对象；
  ② `sim.deleted_ids` + `live_objects()`：**逻辑删除**视图 —— 网格/边界/解场抽取器立即看不到被删对象（撤销只需清空集合）；
  ③ `sim_writer.remove_object_lines()`：**物理删除** —— 按 `obj.line` 定位对象行整段切除，并平移其后所有对象的 `line` 与数组载荷 `start`（此前 `deleted=` 只让补丁跳过，原始行仍留在文件里）；`SessionDocument.clear_volume_mesh()/restore_volume_mesh()` 串起标记+解引用+清缓存。
  实测（官方 vortexShed 算例，4740 对象）：清除后 `extract_volume_mesh()` → ok=False；**保存→重开后仍 ok=False 且对象数减少 233**（拓扑组/场组/存储对象）；`restore_volume_mesh()` 后抽取恢复 ok=True。测试 `tests/test_mesh_clear.py` 4 项全绿。
  **诚实边界**：网格>转 2D 的内核级转换（把体网格重编码为官方的边式 2D 存储）**仍未做**，仍标 needs_kernel —— 该操作需要重写 Coord/VertexList/FaceCellIndex 的存储编码，风险高，本轮未动。
### S5 原始目标（存档）
- **目标**：① Mesh>清除/转 2D 本地实现（消除 needs_kernel 两项）；② 3D-CAD 界内 B-Rep 建模接 GUI（OCC 内核已在 C2/C3）；③ 打包：装 PyInstaller 或走官方 installer 生成真实二进制。
- **验收**：star_gui_parity.md 对应行从 needs_kernel/降级 变为 persist/view；GUI 测试全绿 + 新增 3 项动作测试。

### S6 官方参考数据语料库与自动对标（P2，依赖 F1）—— ⚠️ 第一轮达成（清单+抽取+判定+报告；官方"受控运行"已验证）

**已交付**：
- `benchmarks/manifest.json`：**3 个官方算例**（Re=200 主算例 / Re=100 / Re=200 分段），含参数、容差（`st_rel`/振幅比带）、出处；官方参考量**不写死**，由 `--extract` 现场从 .sim 抽取。
- `bench_report.py`：`load_manifest`（校验）/ `extract_reference`（现场抽取 St·振幅·残差，内存不足诚实跳过）/ `compare`（四条判定路径：**通过 / 未通过 / 未提供 / 不可比**）/ `run_bench` / `render_report` / CLI（`--manifest --ours --no-extract --out --json`）。
- `benchmarks/ours_cylinder.json`：S2 自研结果的**诚实记录**（未达周期 → St=null + 原因），使报告能真实演示"不可比"。

**实测（`python bench_report.py --ours benchmarks/ours_cylinder.json --out benchmarks/report.json`）**：

| 算例 | 官方参考（现场抽取） | 自研 | 判定 |
| --- | --- | --- | --- |
| `cyl_re200_u005`（U=0.05/200 s） | **St=0.1752**（FFT 0.1760 / 过零 0.1744）、振幅 0.2805、残差 3.206e-10、Re=200 | 未达脱落周期（PISO(2)+limited+slip） | **不可比**（不计通过） |
| `cyl_re100_u0025`（U=0.025） | **St=0.1407**（FFT 0.1440 / 过零 0.1374）、振幅 0.8126、残差 2.521e-12、Re=100 | 未提供 | **未提供** |
| `cyl_re200_seg`（分段 247590 迭代） | **St=0.1749**（FFT 0.1745 / 过零 0.1752）、振幅 0.289、残差 1.774e-10 | 未提供 | **未提供** |

**顺带得到的官方内部一致性证据**：两个独立 Re=200 官方运行（主算例 vs 分段算例）的 St 相差 **0.17%**（0.1752 vs 0.1749）→ 官方参考量本身可靠，可直接作为对标基准。

**官方"受控运行"能力（本轮实测打通，S6 生成侧）**：`star_bridge.official_run_case` —— 在**工作副本**上跑官方求解：可选 `generateVolumeMesh()` / `clearSolution()`，跑到目标迭代数即 `stop()` 再 `saveState()`。宏内只用**已核对 API**（`sim.getSimulationIterator()` / `it.run()/isIterating()/stop()/getCurrentIteration()`）；实测 `pipeBlockage` 副本 0→100 迭代后落盘成功（`BRIDGE_RUN_DONE`）。**粒度如实说明**：轮询间隔 200 ms，快速算例会过冲（目标 5 → 实际 100 迭代）；另注 `sim.getSolver()` 在本版本**不存在**（实测编译失败），已在模块注释中固化，避免再写错。


**本轮追加（官方桥生成侧）**：
- `star_bridge.official_run_case`（受控官方运行，仅用 Javadoc 核对过的 API）实测：`pipeBlockage` 副本 0→100 迭代落盘成功；轮询间隔收窄到 **50 ms**（如实记录粒度）。**实测否证**：`sim.getSolver()` 在本版本不存在（编译失败），已在模块注释固化。
- **新生成的官方参考**：`optimate/data/airfoil.sim`（三元素翼型，原本无解）经官方桥 `clearSolution` → `SimulationIterator.run()` 跑到**算例自身停止准则 600 迭代**（90 s）→ `benchmarks/official/airfoil_official_2000.sim`。末段：**Cl=2.2445（±0.0047）、Cd=0.0756（±0.0028）、Cl/Cd=29.73、Continuity 1.07e-3** —— 这批数据原先在本项目里是"缺失的官方翼型解"。
- 清单扩到 **4 算例**（3 官方 .sim + 1 官方桥现场生成），新增**稳态力系数类**判定（`metrics: ["cl","cd"]` + `cl_rel/cd_rel` 容差）；非脱落型算例**不报 St**（避免把按迭代索引的曲线当脱落频率）。
- `tests/test_bench_report.py` 增至 **7 项**（含稳态力系数四路径）。
**仍开放**：① 官方侧建模宏（把我们自己的通道域算例喂给官方跑，做真正的同网格对照）；② 自研侧尚未有任何"通过"项（S2 未复现脱落）；③ `.simh`/解场级对标未做。
### S6 原始目标（存档）
- **目标**：用官方 STAR-CCM+ 批量生成参考语料（同几何的网格尺寸/质量、稳态/瞬态解、报告值、重存文件），落成 benchmarks/ + 自动对标脚本，把 R4/R5 的"对标"从一次性变为可重复。
- **手段**：宏模板（建网格/求解/导出报告/保存）+ 清单 JSON（工况、期望指标、容差）+ bench_report.py（跑批 + 汇总 + 对比）。
- **验收**：≥3 个工况（圆柱/直管/翼型）有官方参考与自研对比报告；容差表固定进文档。

## 4. 建议顺序与依赖

| 顺序 | 内容 | 依赖 | 预估 |
| --- | --- | --- | --- |
| 1 | **S1** 官方桥实战 + 官方差分（W6 真跑） | F1 | 1 波 |
| 2 | **S2** 涡脱复现（二阶格式 → 棱柱层 → AMR → 长积分） | S1（可选） | 2–3 波 |
| 3 | **S3** 字段级归属（T 串/数组/校验和/NameManager/v3_0.025） | S1（官方对照） | 2 波 |
| 4 | **S4** 网格-求解联调与性能 | S2 | 1–2 波 |
| 5 | **S6** 官方参考语料库与自动对标 | S1 | 1 波（可与 S2 并行） |
| 6 | **S5** 客户端收口 | — | 1 波 |

## 5. 风险与对策

| 风险 | 对策 |
| --- | --- |
| 官方许可为单席位/并发受限，批量对标可能排队 | 参考语料一次生成后归档；宏跑批串行 + 失败重试；许可不足 auto-skip 并记录 |
| S2 投入大且可能仍不达标（数值耗散根因） | 先做①二阶格式（改动小）+④长积分验证；不达标即出结论，不无限追加 |
| S3 归属推断过拟合（命中率误导） | 强制"证据类型 + 反例"双报；跨 3 文件交叉验证 |
| 性能改造侵入面大（fvm_core 是 P4–P12 共同底座） | 先 profile 找热点；改动用开关 + 全量回归门控 |
| 文档分数与实际能力脱节 | 每波结束按 §1 表重新评级（允许下调，如本轮求解器 82%→81%） |

## 6. 一句话结论

当前代码在外壳/会话/落盘层已接近对标（完整度约 89%），深度约 80% 的瓶颈集中在三处：解析字段级语义、求解复现能力、自动化/协同；而本机官方 license 可用这一新事实，使官方差分与官方参考数据从挂起项变为最高性价比切入点 —— 因此 S 波以 S1（官方桥）开局、以 S2（涡脱复现）为靶心。


